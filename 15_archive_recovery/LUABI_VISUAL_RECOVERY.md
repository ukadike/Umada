# Lùabi visual recovery record

**Recovery status:** source image not yet present in repository  
**Do not fabricate a substitute.**

## Confirmed creator direction

The Lùabi device has an established visual/field-operations direction outside the current repository image set. The confirmed direction is a monochrome, hand-drawn technical ledger board showing the device as an investigative field object rather than a generic science-fiction prop.

The recovered direction includes:

- orthographic / technical-ledger presentation;
- hand-held human scale;
- field camera, microphone, location and environmental sensing functions;
- explicit consent-state control;
- haptic and visual status signaling;
- local/evidence storage and structured record output;
- accessibility modes treated as device behavior, not decoration;
- use in the Friedmandostorp / Cape Wipeout field context.

## Repository handling rule

Until the original approved image is recovered and supplied as a file, this record is metadata only. It does **not** authorize regeneration, reinterpretation, replacement art, or promotion into `VISUAL_CANON_REGISTRY.md` as a new asset.

When the source file is recovered:

1. add the original image under an appropriate `assets/` path without altering it;
2. record filename, provenance, date, and creator approval;
3. add alt text / long description;
4. register the artifact in the visual-canon registry and structured visual artifact data;
5. only then surface the image in the public site or interactive experience.

## Why this record exists

The project has reached a stage where earlier visual decisions must not disappear simply because the corresponding binary asset has not yet been ingested. This recovery note preserves the decision while keeping the missing source visible as a gap rather than silently filling it.
